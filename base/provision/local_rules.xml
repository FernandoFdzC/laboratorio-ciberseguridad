<group name="local,syslog,ids,attack,">
  <!-- ==================== REGLAS DE COLDDBOX ==================== -->
  
  <!-- Regla de intento de login (31509): se activa cada vez que hay un intento de inicio de sesión -->
  <rule id="31509" level="3" overwrite="yes">
    <if_sid>31509</if_sid>
    <description>Intento de inicio de sesión en WordPress (ColddBox)</description>
  </rule>

  <!-- Regla de fuerza bruta (31510): se activa cuando se detecta un ataque de fuerza bruta (múltiples intentos) -->
  <rule id="31510" level="8" overwrite="yes">
    <if_sid>31510</if_sid>
    <description>Ataque de fuerza bruta en WordPress (ColddBox) - Flag: C2liZXJzZWd1cmlkYWQ=</description>
  </rule>

  <rule id="5305" level="4" overwrite="yes">
    <if_sid>5305</if_sid>
    <description>Acceso al usuario c0ldd - Flag: Q2liZXJzZWd1cmlkYWQ=</description>
  </rule>

  <rule id="5402" level="3" overwrite="yes">
    <if_sid>5402</if_sid>
    <description>Escalada a root exitosa - Flag: RlRQ</description>
  </rule>

</group>